// import { GameTypes } from "../consts/gameTypes";
// import { MEMORY_GAME_EVENTS } from "../consts/memoryGameEvents";

// export function emitStartMemoryGame(emit, { roomKey, players }) {
//   emit(MEMORY_GAME_EVENTS.START, {roomKey: `${roomKey}/${GameTypes.MEMORY_GAME}`, players });
// }

// export function emitPlayerScored(emit, { roomKey, userId }) {
//   emit(MEMORY_GAME_EVENTS.PLAYER_SCORED, { roomKey, userId });
// }

// export function emitTurnEnded(emit, { roomKey }) {
//   emit(MEMORY_GAME_EVENTS.TURN_ENDED, { roomKey });
// }
